# Copy to .env for local dev, or set as an environment variable on your host.
# Points the storefront + admin dashboard at your backend API.
VITE_API_URL=http://localhost:8000

# Razorpay public key (KEY_ID only — never put KEY_SECRET here):
VITE_RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxxx
